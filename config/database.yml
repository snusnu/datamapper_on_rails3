defaults: &defaults
  adapter: mysql
  username: root
  password:
  host: localhost

development:
  database: rails3_development
  <<: *defaults

test:
  database: rails3_test
  <<: *defaults

production:
  database: rails3_production
  <<: *defaults
